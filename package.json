{
  "name": "skane-trafiken-schedule",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "deploy": "nuxthub deploy",
    "deploy:preview": "nuxthub deploy --preview",
    "deploy:wrangler": "npx wrangler pages deploy dist --project-name=skane-trafiken-schedule"
  },
  "dependencies": {
    "@internationalized/date": "^3.11.0",
    "@nuxt/ui": "^3.0.0",
    "@nuxthub/core": "^0.10.0",
    "drizzle-orm": "^0.38.0",
    "nuxt": "^3.15.0",
    "resend": "^4.0.0",
    "ulid": "^2.3.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.64.0"
  }
}
